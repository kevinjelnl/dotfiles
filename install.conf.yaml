- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.shell:
    ~/.gitignore_global:
    ~/.tmux.conf: tmux.conf


- shell:
  - [git submodule update --init --recursive, Installing submodules]
